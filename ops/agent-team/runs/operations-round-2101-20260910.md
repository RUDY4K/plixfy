# جولة تشغيل Plixfy — 10 سبتمبر 2026، 21:01 الرياض

## الهدف والنطاق

- جولة المساء لعمليات المحتوى والجودة وخلاصة اليوم.
- مراقبة `Campaign #1` دون تغييرها.
- تشخيص فشل Cloud Social وإعداد إصلاح محلي معزول واختبارات ومراجعة مستقلة.
- فحص حي ثنائي اللغة على الكمبيوتر والجوال وصفحة لعبة فعلية.

## Google Ads

- بقيت `Campaign #1` متوقفة يدويًا، وتعرض اللوحة رسالة أن الإعلانات لا تعمل.
- لم تتغير الأرقام منذ الظهيرة: 77 ظهورًا، 12 نقرة، CTR 15.58%، تكلفة 67.06 ر.س، ومتوسط CPC 5.59 ر.س، والتحويلات 0.
- لا إنفاق إضافي ظاهر بعد الإيقاف، مع بقاء تنبيه أن تقارير Google ليست وقتية.
- لم تُعد الحملة للعمل ولم تتغير الميزانية أو الكلمات أو الشبكات أو التحويلات.

## عمليات المحتوى

- أحدث Content Drafts، التشغيل `34492189241`، نجح من 14:54:58Z إلى 14:56:20Z. أعد مسودات للمراجعة وتحقق من عدم تغير الملفات المنشورة؛ لا يثبت نشرًا حيًا.
- سجلا اعتماد الأخبار والمدونة في `origin/main` فارغان، و`news.json` الخام يحتوي 60 عنصرًا وآخر `publishedAt` فيه 5 سبتمبر. لا يوجد خبر معتمد أو حي جديد.
- لقطة الطابور المحلية المتاحة تحتوي 85/85 بحالة `pending_review`، لكنها أقدم من تشغيل المساء فلا تُعامل كعدد آني.
- المرشح `d709467` لم يدخل `origin/main` وأصبح متشعبًا عن الأساس الحالي `07cd388`؛ لا يُرفع كما هو ويحتاج إعادة تطبيق واختبار فوق الأساس الأحدث.

## عطل Cloud Social وإصلاحه المحلي

- أحدث تشغيل Cloud Social، `34507437252`، فشل من 17:18:52Z إلى 17:19:13Z.
- وصل TrendAgent إلى 10 اتجاهات حية، ثم توقف المشغّل قبل PublisherAgent بالخطأ: لا يوجد منشور أخبار عام مسجل خلال 24 ساعة. لا دليل على فشل Buffer أو بيانات الاعتماد في هذا التشغيل، ولا دليل على نشر ناجح.
- السبب الجذري: `cloud-social-runner.mjs` يقبل أخبارًا معتمدة فقط، وعند غيابها 24 ساعة يرمي خطأ بدل استخدام صفحة Plixfy أصلية معتمدة كما تقضي خطة التشغيل.
- أُنشئ إصلاح محلي معزول في `C:\Users\gaming\plixfy-social-fallback-20260910`، الفرع `codex/social-fallback-20260910`، عند `70c92d9e6391cb41d139c8ec07c1af543dfc02a9` فوق `origin/main` `07cd388`.
- يختار الإصلاح بعد 24 ساعة صفحة من allowlist أول-طرف فقط: دليل ألعاب المتصفح أو جميع الألعاب أو فئة الألغاز. الأخبار نفسها تظل محجوبة خلف `getPublishedNews` ولا يفتح أي خبر غير معتمد.
- يفرض منشورًا واحدًا لكل قناة في اليوم ومنع تكرار الرابط سبعة أيام، ويدمج سجل المنشورات اليدوية من أصل `https://www.plixfy.com` لأغراض الحد فقط. سُجل منشور X اليدوي في 9 سبتمبر كي لا يتكرر رابط الدليل.
- عند فشل قناة جزئي، يحفظ نجاح القنوات وحالة `partial` ثم يخرج بكود 1 لتنبيه workflow؛ إعادة اليوم التالي تستهدف القناة الفاشلة فقط ولا تعيد الرابط للقناة الناجحة.
- تحقق المدير حديثًا: `npm test` = 136/136 ناجحة، و`npm run social:preflight` نجح في وضع offline dry-run واختار `all-games` لخمس قنوات مع 0 نشر حي، و`git diff --check` نظيف وحالة worktree نظيفة.
- المراجع المستقل أعاد إنتاج ثلاثة عيوب P1 في جولتين، ثم تحقق من إصلاحها وأصدر PASS نهائيًا لـ`70c92d9`.
- لم يحدث push أو PR أو merge أو deploy. المرشح لن يؤثر في الأتمتة الحية حتى موافقة المالك على النشر ونطاق القنوات.

## فحص الجودة الحي

- سطح المكتب 1440×900 والجوال 390×844: الرئيسية، جميع الألعاب وفئة الألغاز بالعربية والإنجليزية أعادت 200 دون overflow؛ `lang/dir` وcanonical والعناوين محلية صحيحة.
- البحث عن `Obby` بالعربية والإنجليزية حفظ `?q=Obby` وأظهر 103 نتائج وروابط لعب محلية.
- صفحة لعبة فعلية بالعربية والإنجليزية أعادت 200 و`noindex, follow` المتوافق مع بوابة صفحات الكتالوج. مسار launch أعاد 200 وiframe متجاوب.
- وصل تشغيل `Obby: All Games Dash or Die` إلى واجهة اللعبة الفعلية على الكمبيوتر؛ تحقق الجوال حتى iframe المتجاوب.
- صفحات 404 العربية والإنجليزية أعادت HTTP 404 فعليًا مع `noindex,nofollow` وروابط عودة.
- `robots.txt` و`sitemap.xml` و`ads.txt` سليمة؛ sitemap يحتوي 38 URL فريدة، وads.txt يحتوي معرف `pub-7564871953180369`.
- CSP ما زالت `Content-Security-Policy-Report-Only` ولا توجد سياسة مفروضة.
- مشكلة P2 غير حاجبة: أخطاء cross-origin من Playgama/منظومة الإعلانات وطلب Yahoo sync 400 بعد بدء اللعبة، لكن اللعبة استمرت حتى واجهتها.

## خلاصة اليوم والخطوة التالية

1. الموقع وتجربة اللعب والفهرسة الأساسية سليمة؛ لا P0 أو P1 في الفحص الحي.
2. الحملة متوقفة يدويًا ولا تُستأنف دون موافقة جديدة.
3. تحويل Ads و`google / cpc` ما زالا P1 قياس منفصلًا؛ لم تتغير إعداداتهما.
4. عطل السوشال شُخّص وأُصلح محليًا مع اختبارات ومراجعة، لكنه غير منشور. يحتاج موافقة صريحة قبل push/PR/merge/deploy لأن تفعيله سيعيد النشر المجدول إلى Telegram وDiscord وX وFacebook وInstagram.
5. الأخبار الحية ما زالت متوقفة بسبب غياب اعتماد تحريري؛ fallback يعالج استمرارية التوزيع ولا يحوّل المسودات إلى أخبار معتمدة.

## القيود

- لا تعديل إعلان أو ميزانية أو تحويلات أو إنتاج.
- لا نشر اجتماعي حي في هذه الجولة.
- تغييرات التطبيق محصورة في worktree مستقل؛ checkout الرئيسي بقي محميًا.
