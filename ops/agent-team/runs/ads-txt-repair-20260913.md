# إصلاح ads.txt — 13 سبتمبر 2026

## الإشارة

- لقطة المالك عند 14:54 الرياض أظهرت موقع `plixfy.com` بحالة `Getting ready` وحالة ads.txt‏ `Not found`، وآخر تحديث في اللوحة 5 سبتمبر 2026.

## التحقيق الحي

- `https://plixfy.com/ads.txt` يعيد 308 إلى `https://www.plixfy.com/ads.txt`.
- الوجهة canonical تعيد 200 و`text/plain; charset=utf-8`، وكذلك عند استخدام `Mediapartners-Google` و`AdsBot-Google`.
- `robots.txt` يسمح بالوصول ولا يحظر `/ads.txt`.
- التحويل الواحد من النطاق الجذري إلى `www` مدعوم وفق دليل Google الرسمي، لذلك ليس عطلًا مؤكدًا.
- الملف الحي يحوي سطر Plixfy الصحيح، لكنه يحوي 49 سطرًا إضافيًا لبائعين قدامى. حالة `Not found` تعكس آخر زحف في AdSense ولا تثبت أن الملف يعيد 404 الآن؛ لوحة المستخدم نفسها لم تتحدث منذ 5 سبتمبر.

## السبب الجذري

- تنظيف البائعين كان موجودًا في commit محلي تاريخي `a721486`، لكنه ليس ancestor لـ`origin/main` ولم يُدمج.
- أحدث `origin/main` عند التحقيق `ae0c79d0dd025a418ae63e3751693d8c33e4d4ed`، وما زال يحمل ملف الخمسين سطرًا؛ مزامنة الكتالوج لم تنشئ المشكلة لكنها نشرت أحدث main بنفس الملف القديم.
- مراقب الإنتاج كان يتحقق فقط من وجود سطر Plixfy، ولذلك مرر الملف رغم وجود بائعين غير مصرحين.

## المرشح المحلي

- worktree: `C:/Users/gaming/plixfy-ads-txt-repair-20260913`
- branch: `codex/ads-txt-repair-20260913`
- commit: `c3ecaab7b5dfe8252fdfda3d6eb26552e7207eed`
- جعل `public/ads.txt` سطرًا واحدًا فقط:
  `google.com, pub-7564871953180369, DIRECT, f08c47fec0942fa0`
- شدد `validateAdsTxt` ليرفض أي بائع أو تكرار إضافي، وأضاف اختبارًا يمنع رجوع القائمة القديمة.

## التحقق

- الاختبار قبل الإصلاح: فشل كما هو متوقع لأن المراقب قبل بائعًا إضافيًا.
- بعد الإصلاح: 144/144 اختبارًا ناجحًا.
- `npm run build`: ناجح، 623/623 صفحة.
- تشغيل محلي للإنتاج: `/ads.txt` أعاد 200 و`text/plain` و59 بايتًا، والتحقق أعاد `sellerLines: 1`.
- `git diff --check`: ناجح.
- مراجعة مستقلة: PASS بعد إضافة رفض تكرار السطر نفسه.

## الحالة والحد الخارجي

- المرشح committed محليًا ونظيف، ولم يُرفع أو يُدمج أو يُنشر.
- لم يُضغط `Check for updates` في AdSense؛ ينفذ بعد نشر الملف الصحيح لأنه طلب إعادة زحف خارجي.
- الخطوة التالية تحتاج موافقة المالك الصريحة: push ثم PR/merge ونشر Vercel، تحقق حي، ثم طلب `Check for updates` في AdSense. تغيير اللوحة قد يستغرق أيامًا، وقد يطول في المواقع قليلة طلبات الإعلان وفق Google.
