# مراجعة جاهزية الإطلاق — 7 سبتمبر 2026

- الهدف: مراجعة وتجهيز Plixfy للإطلاق محليًا فقط، مع الحفاظ على التعديلات القائمة. لا نشر أو DNS أو إعلانات أو بيانات اعتماد أو خدمة إنتاج.
- نطاق الاختبار: العربية والإنجليزية، جوال وكمبيوتر، لعبة، بحث، فئات، حساب ومفضلة، موافقة التحليلات، 404، robots/sitemap/ads.txt، كتالوج، CSP، بناء واختبارات.
- وضع التنفيذ: الملاحظات والفحص من checkout الرئيسي المتسخ؛ أي اختبار تغيير في worktree منفصل من `origin/main` ولا يدمج تلقائيًا.
- مصدر الحقيقة: checkout المحلي 2,040 لعبة ومزامن 26 أغسطس؛ `origin/main` عند `84713a9` والإنتاج الحالي متطابقان على 2,125 لعبة، ومزامنة 7 سبتمبر. بيانات upstream هي الأحدث؛ لا يجوز نسخ البيانات المحلية القديمة فوقها.
- الفريق: catalog_truth، functional_bilingual_audit، csp_safe_audit؛ جميعهم قراءة فقط، بلا أسرار أو نشر. مراجعة مستقلة مطلوبة لأي كتابة.
- تحقق الوظائف: راجع الفريق AR/EN عند 390px و1280px، والبحث والفئات والمفضلة المحلية والضيف ورفض التحليلات ولعبة Playgama وملفات robots/sitemap/ads. اجتازت نسخة checkout الحالية 21 اختبار موقع و7 تشغيل و4 محتوى وبناء621؛ ونسخة مرشحة نظيفة اجتازت53 اختبارًا وبناء Webpack لـ623 صفحة.
- إصدار منشور: أُنشئ worktree من `origin/main` الأحدث عند `cf829c2` بكتالوج2,125، ونُقلت فقط حزمة 404 وحفظ query الآمنة؛ بقية التعديلات المحلية القديمة محفوظة في checkout الأصلي ولم تنقل لأنها متجاوزة أو تتراجع عن إصلاحات أحدث. اجتازت119 اختبارًا وبناء623 ومراجعة مستقلة. نُشر `66b3bd8` ثم التصحيح `fcffabe` لمنع فهرسة404؛ تحقق الإنتاج AR/EN وnoindex والبحث ناجح.
- CSP: تجربة فرض آمنة في worktree منفصل نجحت للبناء والمسارات الأساسية وإطار اللعبة بلا حجب CSP على الصفحة الأم؛ لم تختبر AdSense حقيقيًا ولا توجد تقارير مخالفات. بقيت الإنتاج على Report-Only ولم يعتمد التغيير.
- القرار: غير جاهز للنشر من checkout المتسخ الحالي لأنه متأخر71 commit. المطلوب تسوية تعديلات المالك فوق `origin/main` الأحدث، ثم إدراج الإصلاح المرشح وإعادة التحقق؛ فرض CSP مؤجل لاختبار staging بإعلانات وتقارير مخالفات.
- الحالة: مكتمل محليًا؛ لا تغيير خارجي.
