# تشغيل continuous-009

- التاريخ: 2026-08-29
- المدير: `plixfy-manager`
- الحالة: `completed_locally`
- الأساس: `42f12ecdb16c030c02fd7feee0e7b87e6c8ce905`
- worktree: `C:\Users\gaming\plixfy-continuous-009`
- branch: `manager/growth-quality-20260829`
- وضع التنفيذ: قراءة وتحليل ثم كتابة مستودع معزولة قابلة للتراجع؛ بلا أسرار أو
  push أو PR أو merge أو deploy أو نشر أو إرسال أو إنفاق أو تغيير حساب.

## الهدف

اختيار أعلى فرصة قابلة للقياس لتحسين نمو Plixfy وجودته بعد إغلاق دورة موثوقية
الحساب، وتنفيذها محليًا مع اختبارات ومراجعة مستقلة دون أي أثر خارجي.

## الاستقبال والافتراضات

- النتيجة: إصلاح مشكلة مثبتة أو فتح فرصة نمو واضحة في SEO أو UX أو الأداء أو
  القياس أو الإيراد، مع دليل قبل/بعد.
- البيانات: المستودع والمرشح المحلي هما المصدر الأساسي؛ البحث العام قراءة فقط.
- خارج النطاق: الإنتاج، الحسابات، الإعلانات، النشر الاجتماعي، الإنفاق، الحذف.
- الميزانية: لا استدعاءات مدفوعة؛ ثلاث مهام تدقيق متوازية ثم موجة تنفيذ ومراجعة.
- معيار الاختيار: أولوية P0/P1، ثم أثر مستخدم أو نمو قابل للقياس، ثم أقل مساحة
  تغيير وأقوى إمكانية تحقق محلي.

## الموجة الأولى

1. تدقيق SEO والفهرسة والربط والتحويل العضوي على المرشح الحالي.
2. تدقيق رحلة الاكتشاف واللعب والاحتفاظ بالعربية والإنجليزية والجوال والكمبيوتر.
3. تدقيق الصحة التقنية والأداء والقياس والإيراد والامتثال دون قراءة أسرار.

## بوابات الموافقة

- أي push أو PR أو merge أو deploy يحتاج موافقة صريحة مستقلة.
- أي تغيير إنتاجي أو قاعدة بيانات أو إعداد حساب أو إعلان أو إرسال خارجي متوقف.

## النتيجة المنفذة

- commit المرشح النهائي: `b930929` فوق `321bee2` على
  `manager/growth-quality-20260829`، والعملtree نظيف.
- حُمي قياس GA من نص البحث وquery/hash في الأحداث والسياق والطابور ومفاتيح
  `sessionStorage`، وأصبح سحب الموافقة يمسح runtime والطابور وdedup مع reload
  محروس فقط إذا سبق تحميل runtime.
- أضيف timeout تحميل اللعبة بعد 20 ثانية مع خطأ عربي/إنجليزي، retry ومحاولة
  iframe جديدة ومنع callbacks القديمة والتكرار. الحدث الدقيق هو
  `game_frame_loaded` لأنه يثبت تحميل مستند iframe لا جاهزية اللعبة الداخلية.
- تُزال iframe المتعثرة قبل إعادة المحاولة، مع إبقاء `game_start` والسجل مرة
  واحدة لكل جولة.
- صار الربط التحريري بحسب اعتماد اللغة: الرئيسية/المركز يربطان 29 دليلًا عربيًا
  وخمس صفحات إنجليزية معتمدة فقط، بلا قطع رحلة الإنجليزية إلى العربية وبلا
  إضافة روابط في صفحات `/like`.
- صُححت ادعاءات دعم الأجهزة لتطابق الكتالوج: 1977 للجوال والكمبيوتر، 92
  للكمبيوتر فقط، و5 للجوال فقط. حُذفت أنظمة التشغيل غير المثبتة من JSON-LD.

## التحقق والمراجعة

- البيئة النظيفة `plixfy-cycle9-verify-b930929` بلا `.env` أو `.private`:
  `npm ci --include=dev` ثم `npm test` = 202/202 بلا فشل أو تخطٍ.
- بناء Next.js 16.2.12 الافتراضي Turbopack نجح مع 623/623 صفحة، واختبارات
  الأداء على خادم localhost معزول 3/3.
- `git diff --check` ناجح، ولا عمليات Node أو Vivaldi معزولة باقية.
- المراجعة المستقلة النهائية: PASS؛ P0/P1/P2/P3 = صفر ضمن النطاق.
- محاولة Vivaldi headless المعزولة علقت بلا لقطة؛ أوقفت عملياته المطابقة لمسار
  الملف المؤقت فقط، ولا يوجد ادعاء بفحص بصري جديد في هذه الدورة.

## الأثر الخارجي

- لا push أو PR أو merge أو deploy أو نشر أو إرسال أو إنفاق أو تغيير حساب.
- المرشح المحلي ينتظر موافقة مستقلة على كل خطوة إصدار خارجية ونطاقها.
