# تشغيل continuous-008

- التاريخ: 2026-08-29
- المدير: `plixfy-manager`
- الحالة: `completed_locally`
- الأساس: `a721486363954f0b43803757b9078502e6502707`
- worktree: `C:\Users\gaming\plixfy-continuous-008`
- branch: `manager/reliability-ux-20260829`
- وضع التنفيذ: تدقيق وكتابة مستودع معزولة قابلة للتراجع؛ بلا أسرار أو push أو
  PR أو merge أو deploy أو رسالة أو إنفاق أو تغيير حساب أو حذف مادي.

## الهدف

إغلاق أعلى ديون الموثوقية المتبقية في مزامنة بيانات الحساب، وإدارة Service
Worker القديمة، وبوابة CI قبل اقتراح الإصدار التالي، دون تغيير سلوك الإنتاج.

## معيار النجاح

1. إثبات الخلل من المسار الفعلي والاختبارات، لا من النصوص فقط.
2. تنفيذ الإصلاحات الآمنة ذات الأثر القابل للقياس بملكية ملفات غير متداخلة.
3. مراجعة مستقلة لكل تغيير ونجاح الاختبارات والبناء المناسبين.
4. إبقاء أي workflow أو نشر أو حذف ملف أو أثر حسابي خلف موافقة مالك مستقلة.

## نطاق الموجة الأولى

- تدقيق مزامنة Supabase وحالة النجاح/الفشل المعروضة للمستخدم.
- تدقيق تسجيل وإلغاء Service Workers وحدود ملكية Plixfy لعمليات unregister.
- تدقيق غياب CI للـPR وتحديد أقل بوابة محلية آمنة قبل أي نشر workflow.

## التنفيذ

- عُزلت بيانات الضيف وكل حساب في مفاتيح cache/outbox مستقلة، وأُلغي الرفع
  التلقائي لبيانات الضيف. الاستيراد إلى الحساب أصبح إجراءً صريحًا، ولا تُمسح
  نسخة الضيف إلا بعد نجاح القراءة والكتابة كاملة.
- أضيفت حالات مزامنة حقيقية `idle/syncing/synced/error`، وفُحصت أخطاء Supabase
  المهيكلة والاستثناءات، مع retry وحواجز epoch وهوية تمنع نتيجة حساب قديمة من
  الكتابة فوق حساب جديد.
- أصبحت نوايا المفضلة حتمية وآمنة أثناء الطلبات الجارية عبر إقرار يقارن snapshot
  بالقيمة الحالية، مع حد 100 دون eviction صامت. أضيف trigger بقاعدة البيانات
  لفرض الحد تحت advisory lock وtrigger يمنع رجوع `played_at` للخلف.
- حُصر تقاعد Service Worker في التسجيل التاريخي exact `/sw.js` ذي root scope،
  وحُفظت التسجيلات الأخرى والترقيات المختلطة. استُبدل runtime Monetag القديم
  و`sw.js` بـtombstones خاملة، وقُفل سكربت إعادة التفعيل قبل أي شبكة أو كتابة.
- أضيف workflow PR CI مقروء فقط، actions فيه مثبتة SHA، ويشغل التثبيت والاختبارات
  ثم يرفض أي فرق tracked أو staged أو ملف untracked.

## المراجعة والتحقق

- الالتزام المحلي: `42f12ec` على `manager/reliability-ux-20260829`، والشجرة
  نظيفة. لم يحدث push أو PR أو merge أو deploy أو تغيير حساب/قاعدة بيانات.
- نسخة تحقق مؤقتة نظيفة بلا `.env`: نجح
  `npm ci --ignore-scripts --no-audit --no-fund`، ثم 187 اختبارًا عاديًا و3
  اختبارات أداء بلا تخطٍ، وبناء Next.js Turbopack لـ623/623 صفحة.
- وقع خروج Windows عابر لعامل Next أثناء أول تشغيل مجمع للأداء بعد اكتمال
  compile وtypecheck؛ الإعادة المباشرة لبوابة الأداء والبناء نجحت 3/3 و623/623.
- `git diff --check` وحواجز tracked/staged/untracked نجحت، والمراجعات المستقلة
  النهائية لم تجد P0 أو P1 أو P2 أو P3.
- فحص Vivaldi المعزول علق دون لقطة أو نتيجة؛ أُوقفت عملياته المعزولة فقط، ولذلك
  لا تدّعي هذه الدورة تحققًا بصريًا جديدًا.

## بوابات ما بعد الدورة

1. يحتاج push/PR موافقة صريحة؛ workflow الجديد لا يعمل على GitHub قبل وصوله.
2. قبل نشر عميل المزامنة يجب تطبيق `supabase/schema.sql` في قاعدة الإنتاج ثم
   التحقق من وجود triggers والسياسات وعدد المفضلة لكل مستخدم.
3. إذا وُجد مستخدم يتجاوز 100 مفضلة فلا حذف تلقائي؛ تُرفع الحالة للمالك بقرار
   تنظيف مستقل يحافظ على البيانات.
4. بعد نجاح قاعدة البيانات يلزم merge/deploy منفصلان بموافقة صريحة، ثم تحقق
   حسابين A/B وخروج/دخول ومفضلة وسجل على الجوال والكمبيوتر والعربية والإنجليزية.
