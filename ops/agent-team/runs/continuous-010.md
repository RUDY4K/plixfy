# تشغيل continuous-010

- التاريخ: 2026-08-29
- المدير: `plixfy-manager`
- الحالة: `completed_locally`
- الأساس: `b9309295f1b9b16510c609c4dba89be04da0905c`
- worktree: `C:\Users\gaming\plixfy-continuous-010`
- branch: `manager/retention-auth-20260829`
- وضع التنفيذ: تدقيق قراءة فقط ثم كتابة معزولة قابلة للتراجع؛ بلا أسرار أو
  شبكة مدفوعة أو push أو PR أو merge أو deploy أو نشر أو تغيير حساب.

## الهدف

إغلاق أعلى خلل متبقٍ في موثوقية الحسابات أو الاحتفاظ والتنقل بعد الدورة
التاسعة، مع سلوك عربي/إنجليزي واختبارات سلوكية ومراجعة مستقلة.

## الاستقبال والافتراضات

- النتيجة القابلة للقياس: منع نجاح زائف أو فقد حالة أو قطع رحلة مستخدم مثبت،
  مع اختبار قبل/بعد وبناء نظيف.
- النطاق: Auth recovery/callback، المفضلة والسجل، التنقل بين اللغات، وحالات
  الخطأ المرتبطة؛ يسمح للمدير باختيار الحزمة الأعلى أثرًا بعد التدقيق.
- خارج النطاق: الإنتاج، قاعدة البيانات الحية، الحسابات، الإعلانات، النشر،
  الإنفاق، الحذف، وقراءة `.env*` أو `.private/`.
- مصادر البيانات: المرشح المحلي `b930929` واختبارات المستودع فقط؛ لا أسرار.
- الميزانية: لا استدعاءات مدفوعة؛ ثلاث مهام تدقيق متوازية ثم تنفيذ ومراجعة.

## الموجة الأولى

1. تدقيق Auth والاسترداد والـcallback وحالات الخطأ والخصوصية.
2. تدقيق المفضلة والسجل والتنقل والاحتفاظ على العربية والإنجليزية.
3. تدقيق تشغيل/أمن تكاملي لأعلى مخاطر المتبقي وقابلية الاختبار.

## بوابات الموافقة

- أي push أو PR أو merge أو deploy يحتاج موافقة صريحة مستقلة.
- أي تغيير قاعدة بيانات حية أو حساب أو بيانات اعتماد أو إرسال خارجي متوقف.

## النتيجة المحلية

- أُنشئ المرشح المحلي `af07d5b3540cada31ac10276a7e1cbc2c1f95ca9`
  على `manager/retention-auth-20260829` فوق `b930929`، بلا push أو PR أو merge
  أو deploy أو أثر خارجي.
- أصبحت المصادقة والاستعادة تفشل بوضوح عند أخطاء المزود أو الشبكة، ولا تفرض
  سياسة كلمة مرور الإنشاء على تسجيل دخول حساب قائم. مسار recovery معزول بعلامة
  HttpOnly قصيرة، والكوكيز Secure في الإنتاج مع HSTS وتحويلات no-store/no-referrer.
- تمنع مكتبة المفضلة والسجل الحالة الفارغة الكاذبة والتعليق غير المحدود، وتعرض
  خطأً قابلًا لإعادة المحاولة، وتحفظ query كاملًا عند تبديل اللغة، وتترجم الفئة
  الإنجليزية من `categorySlug` بدل طباعة الاسم العربي.
- أصبح جلب صور البطاقة الاجتماعية مقيدًا بمصادر ومسارات HTTPS معتمدة، مع إعادة
  تحقق كل redirect وMIME صور وحد 12MB. يراقب watchdog نجاح خطوة dry-run المجدولة
  نفسها ولا يعد workflow أخضر تخطى الخطوة دليلًا على الجاهزية أو النشر.
- راجع ثلاثة وكلاء الحزم تقاطعيًا بعد الإصلاحات؛ النتيجة النهائية بلا ملاحظات
  P0 أو P1 أو P2، وأُغلق أيضًا تطبيع جذر `/en` عند وجود query.

## دليل التحقق

- نسخة نظيفة من commit نفسه مع `npm ci`: `npm test` نجح 246/246 بلا skip.
- بناء Next.js 16.2.12 الافتراضي Turbopack وTypeScript نجح، مع 624/624 صفحة.
- ميزانية الأداء الإنتاجية نجحت 3/3، و`git diff --check` نظيف.
- المجموع: hardening ‏133، social ‏31، site ‏28، content ‏4، operations ‏47،
  performance ‏3.
- محاولة Vivaldi مع ملف تعريف معزول لم تُنتج جلسة أو لقطة قابلة للقياس؛ لا
  يوجد ادعاء بفحص بصري جديد، ولم يُفتح متصفح بديل.

## قرار مؤجل يحتاج موافقة

- جدول `recently_played` السحابي قد يحتفظ بأكثر من آخر 12 سجلًا رغم أن الواجهة
  تعرض 12 فقط. الإصلاح المتين يحتاج حذف صفوف قديمة وتطبيق SQL في الإنتاج؛ لم
  يُنفذ لأنه حذف بيانات وتغيير قاعدة حية يحتاجان موافقة صريحة منفصلة.
